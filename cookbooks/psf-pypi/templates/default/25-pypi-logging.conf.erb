# Generated by Chef
# Local modifications will be overwritten

$ModLoad omprog
$ActionOMProgBinary <%= @cdn['process_script'] %>

# Don't filter duplicate lines
$RepeatedMsgReduction off

# Send all logging to a process, then discard it
:app-name, isequal, "<%= @cdn['app_name'] %>" :omprog:
:app-name, isequal, "<%= @cdn['app_name'] %>" ~

# Turn back on filtering duplicate lines
$RepeatedMsgReduction on
